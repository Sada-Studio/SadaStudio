# Public Sada AI rollout

The public Ask Sada frontend is loaded from `/sada-public-ai.css` and `/sada-public-ai.js` on the homepage, work page, and `project.html` template.

Existing generated project pages will inherit the same includes the next time the portfolio is published from the admin, because project pages are rendered from `project.html`.
